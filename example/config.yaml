pullInterval: 5

clusters:
  myproject1:
    groups:
      common:
        urlPattern: "http://127.0.0.1:999/{host}/agent-pull.php"
        basicAuth:
          user: someuser
          password: somepassword
        hosts: 
          - "some-host-name"
          - "some-other-host-name"
  myproject2:
    groups:
      web:
        urlPattern: "http://{host}:9999/agent-pull.php"
        hosts: 
          - "127.0.0.1"

ui:
  host: 127.0.0.1
  port: 42042

metrics:
  statsd:
    enabled: false
    host: "127.0.0.1"
    port: 8125
    prefix: "some.metric.prefix"
  prometheus:
    enabled: true
    
